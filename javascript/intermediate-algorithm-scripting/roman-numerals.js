const romanNumerals = (n) => {
  const numerals = ['M', 'D', 'C', 'L', 'X', 'V', 'I']
  let roman = ''

  return roman
}

console.log(romanNumerals(50)) // 'L'
console.log(romanNumerals(142)) // 'CXLII'

module.exports = romanNumerals
